/* Version 1 */
/*
Justique si el siguiente vector de enteros representa un heap (maximal o
minimal). ¿Qué condición debe cumplir para que lo sea?

|1|4|2|5|3|6|

Explique (texto o código claro) cómo reordenarlo (inplace, sin requerir reservar
memoria extra) para que cumpla la condición. El algoritmo descripto debe
funcionar para cualquier vector de enteros.

Aplique el algoritmo al vector dado para obtener un heap maximal explicando cada
iteración del mismo. En cada paso de algoritmo muestre como queda el
array (marque con un asterisco los elementos que se movieron en el array en ese
paso).





*/
